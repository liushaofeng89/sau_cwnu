<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>

	<constant name="struts.enable.DynamicMethodInvocation" value="false" />
	<constant name="struts.devMode" value="true" />

	<package name="default" namespace="/" extends="struts-default">

		<interceptors>
			<interceptor name="authority"
				class="com.wyhouseservice.interceptor.AuthorizationInterceptor" />

			<interceptor-stack name="authority-stack">
				<interceptor-ref name="defaultStack" />
				<interceptor-ref name="authority" />
			</interceptor-stack>
		</interceptors>

		<action name="login" class="cn.edu.cwnu.sau.action.admin.LoginAction">
			<result name="success">/_login.jsp</result>
		</action>
		
		<action name="checkAuth" class="cn.edu.cwnu.sau.action.admin.LoginAction" method="checkAuth">
			<result name="error">/_login.jsp</result>
			<result name="success" type="redirect">/admin/index</result>
		</action>
		
		<action name="logout" class="cn.edu.cwnu.sau.action.admin.LoginAction" method="logout">
			<result name="success">/index.jsp</result>
		</action>

	</package>

	<include file="admin.xml" />

	<!-- Add packages here -->

</struts>
